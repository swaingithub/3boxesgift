{%- if settings.show_multiple_currencies -%}
<div class="block_currencies d-md-block">

  <div data-toggle="dropdown" class="dropdown-toggle" title="Currencies">
    <span class="pre-currencies"><img src="{%- render 'processCurrencyImage', current_currency: shop.currency -%}" alt="{{shop.currency}}">{{shop.currency}}</span>
    <i class="leofal fa-angle-down"></i>
  </div>
  <ul id="currencies" class="list-unstyled dropdown-menu shadow">
    <li>
      <a data-currency="{{shop.currency}}" class="selected" href="javascript:void(0)" rel="nofollow">
        <img src="{%- render 'processCurrencyImage', current_currency: shop.currency -%}" alt="{{shop.currency}}">
        {{shop.currency}}
      </a>
    </li>
    {%- assign currencies = settings.list_currencies | split:"," -%}
    {%- assign count = currencies.size | minus: 1 -%}
    {%- for i in (0..count) -%}
    {%- assign currency = currencies[i] -%}
    <li>
      <a data-currency="{{currency}}" href="javascript:void(0)" rel="nofollow">
        <img src="{%- render 'processCurrencyImage', current_currency: currency -%}" alt="{{currency}}" />
        {{currency}}
      </a>
    </li>
    {%- endfor -%}
  </ul>
</div>
{%- endif -%}