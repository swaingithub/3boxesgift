<!-- newsletter.liquid -->
{{ 'newsletter.css' | asset_url | stylesheet_tag }}
<div class="newsletter-big" {% if section.settings.show_bg_newsletter %} style="background-color:{{ section.settings.bg_newsletter }};" {%- endif -%}>
  <div class="page-width">
    <div class="section-block section-block--padding">
      <div class="{% if section.settings.heading != blank or section.settings.subheading != blank %}section-block__header {% endif %}text-center title">
        {%- unless section.settings.heading == blank -%}
          <h4 class="h1--mini section-block__title">{{ section.settings.heading | escape }}</h4>
        {%- endunless -%}
        {%- unless section.settings.subheading == blank -%}
          <div class="section-block__subtext">{{ section.settings.subheading }}</div>
        {%- endunless -%}
      </div>
      <div class="grid newsletter-content">
        <div class="grid__item medium-up--two-thirds medium-up--push-one-sixth">
          <div class="form-vertical">
            {% render 'newlester' %}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

  {% schema %}
  {
    "name": "Newsletter",
    "class": "index-section",
    "settings": [
      {
        "type": "checkbox",
        "id": "show_bg_newsletter",
        "label": "Show background newsletter"
      },
      {
        "type": "color",
        "id": "bg_newsletter",
        "label": "Background color"
      },
      {
        "type": "text",
        "id": "heading",
        "label": "Heading",
        "default": "Subscribe to our newsletter"
      },
      {
        "type": "richtext",
        "id": "subheading",
        "label": "Subheading",
        "default": "<p>Promotions, new products and sales. Directly to your inbox.</p>"
      },
      {
        "type": "paragraph",
        "content": "Any customers who sign up will have an account created for them in Shopify. [View customers](/admin/customers?query=&accepts_marketing=1)"
      }
    ],
    "presets": [
      {
        "name": "Newsletter",
        "category": "Text"
      }
    ]
  }
  {% endschema %}
  
